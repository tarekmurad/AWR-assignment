class AppConstants {
  AppConstants._();
}

enum AssignmentStatus {
  delivered,
  in_transit,
  awaiting_pickup,
}

enum AssignmentType {
  drop_off,
  pick_up,
}

enum TrackingStatus {
  delivered,
  in_transit,
  awaiting_pickup,
}
